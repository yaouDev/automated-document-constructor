\documentclass[$documentclass$]{scrartcl} % there are other base classes as well such as article

% pandoc settings
$if(fontfamily)$
\usepackage[$fontfamilyopts$]{fontspec} % Used with XeLaTeX/LuaLaTeX (often Pandoc's default backend)
$else$
\usepackage{lmodern} % Fallback font for pdflatex
\usepackage[T1]{fontenc} % Font encoding for pdflatex
$endif$

% std packages
\usepackage{amsmath,amssymb,amstext}
\usepackage{graphicx}

% setting page margins and dimensions
\usepackage[margin=1in]{geometry}

% placing content
\usepackage{eso-pic}

% hyperlinks
\usepackage{hyperref}

% background image placement - will look where the yml has placed --resource-path (change the last part of the long line to the background)
\AddToShipoutPictureBG{%
  \AtPageLowerLeft{%
    \includegraphics[width=\paperwidth,height=\paperheight,keepaspectratio]{nice_colors.png}%
  }%
}

% watermark placement if needed
% Requires \usepackage{xcolor} if you use color commands like \color{gray!30}
% \usepackage{xcolor}
% \AddToShipoutPictureBG{%
%   \AtTextCenter{% % Positions the watermark in the center of the text area
%     \put(0,0){%
%       \makebox(0,0)[c]{\normalsize\sffamily\color{gray!30}\rotatebox{45}{DRAFT DOCUMENT}}%
%     }%
%   }%
% }


% produces the actual document
\begin{document}

$body$

\end{document}
